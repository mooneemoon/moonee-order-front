import React from 'react';
// import { PaymentApprovalResponse } from '../types/api';
// import * as querystring from 'query-string';
// import styled from '@emotion/styled';

export function Fail(): React.ReactElement {
  // const param = querystring.parse(window.location.search) as unknown as PaymentApprovalResponse;

  return (
    <div>주문 실패</div>
  );
  // return (
  //   <Wrapper>
  //     { Object.entries(param).map(([key, value]) => (
  //       <h2 key={key}>
  //         { key }
  //         :
  //         { value }
  //       </h2>
  //     )) }
  //   </Wrapper>
  // );
}

// const Wrapper = styled.div`
//   text-align: center;
//   padding-top: 300px;
// `;
